// Command fibe-distilled runs the minimal, single-tenant Fibe server: one binary that
// exposes the Fibe-compatible HTTP API, persists to SQLite, and deploys Docker
// Compose Playgrounds onto the local Docker host represented as a Marquee.
package main
