// Package domain defines fibe-distilled's in-process data contracts.
//
// These structs are the shared language between HTTP handlers, SQLite storage,
// Compose rendering, runtime execution, and workers. JSON tags intentionally
// mirror the compatible Fibe API subset.
package domain
