// Package list implements shared SDK-compatible list query behavior.
//
// It owns strict boolean/exact query parsing, created-at windows, sorting, and
// filtered list response writing for resource handlers.
package list
