// Package response writes Fibe-compatible HTTP response envelopes and errors.
//
// It centralizes JSON encoding, request IDs, list pagination envelopes, and the
// stable structured error shape used by handlers and the compatibility gate.
package response
