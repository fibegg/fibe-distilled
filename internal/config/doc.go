// Package config loads and validates fibe-distilled process configuration.
//
// Configuration comes from environment variables and defines the server
// address, SQLite paths, API token, GitHub token, playguard interval, local
// Docker runtime settings, and the single startup-configured Marquee.
package config
